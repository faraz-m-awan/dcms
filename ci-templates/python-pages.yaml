pages:
  image: python:${PYTHON_VERSION}
  stage: docs
  script:
    - pip install -U sphinx sphinx_rtd_theme
    - pip install .
    - make -C docs html
    - mv build/sphinx/html/ public/
  artifacts:
    paths:
      - public
    only:
      - tags
      - master