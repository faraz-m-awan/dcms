python-test:
  stage: test
  image: python:${PYTHON_VERSION}
  script:
    - pip install pytest pytest-cov .[testing]
    - pytest --cov-report=xml --cov=src tests/ --junitxml=report.xml
  coverage: '/TOTAL.+ ([0-9]{1,3}%)/'
  artifacts:
    when: always
    reports:
      junit: report.xml
